# 🖼️ Imagenette 10类通用物体识别挑战

基于 ResNet-50 的图像分类训练与评测系统

## 📋 项目简介

本项目标准的图像分类训练与评测流程，使用预训练的 ResNet-50 模型在 Imagenette 数据集上进行微调训练。

**数据集类别（10类）**：
- tench（丁鲷）
- English springer（英国激飞长毛猎犬）
- cassette player（磁带播放器）
- chain saw（电锯）
- church（教堂）
- French horn（法国号）
- garbage truck（垃圾车）
- gas pump（加油机）
- golf ball（高尔夫球）
- parachute（降落伞）

## 🚀 快速开始

### 1. 环境要求

- Python 3.8+
- CUDA 11.0+ (推荐使用 GPU)
- 8GB+ 显存 (训练时)

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

### 3. 下载资源（若没有提供数据，请运行此脚本下载数据集，数据将会下载到项目目录下）

```bash
python download_resources.py
```

这将下载：
- **数据集**: Imagenette (ImageNet 的 10 类简化版)
- **模型**: ResNet-50 预训练权重
- **预计大小**: 约 2-3 GB
- **缓存位置**: `./cache/`

### 4. 开始训练

#### 使用 Jupyter Notebook

```bash
jupyter notebook image_classification.ipynb
```

训练完成后，结果将保存在 `./results/` 目录。

## 🎯 训练配置

默认训练参数（可在代码中修改）：

- **批次大小**: 32
- **训练轮数**: 5
- **学习率**: 5e-5
- **优化器**: AdamW
- **图像尺寸**: 224×224

## 📊 评测指标

训练完成后将自动计算以下指标：

- **Accuracy**: 整体准确率
- **Precision (weighted)**: 加权精确率
- **Recall (weighted)**: 加权召回率
- **F1-Score (Macro)**: 宏平均 F1 分数

## 📈 结果输出

训练完成后，`./results/` 目录将包含：

1. **evaluation_results.png**: 综合可视化图表
   - 混淆矩阵
   - 各类别准确率
   - 整体性能指标
   - 真实 vs 预测分布

2. **classification_report.txt**: 详细分类报告
   - 每个类别的 precision/recall/f1-score
   - 整体指标汇总

3. **training_results.json**: JSON 格式的完整结果
   - 训练指标
   - 验证指标
   - 训练时长等元信息

4. **训练日志**: `./resnet50-imagenette/` 目录
   - 模型权重 checkpoint
   - TensorBoard 日志（如启用）

## 🎓 参考资料

- [Imagenette 数据集](https://huggingface.co/datasets/frgfm/imagenette)
- [ResNet-50 模型](https://huggingface.co/microsoft/resnet-50)
- [HuggingFace Transformers 文档](https://huggingface.co/docs/transformers)

## 📄 许可证

本项目仅供学习与比赛使用，请遵守相关数据集与模型的许可协议。

---

**祝训练顺利！🎉**
